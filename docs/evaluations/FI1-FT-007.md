---
slice: FT-007
title: ChipComponent (`<forge-chip>`)
status: pass
date: 2026-05-10
---

# FT-007 — ChipComponent

## Acceptance test
Per the FT-007 entry, no standalone Playwright spec is required — verified inside `workouts-list.spec.ts` (FT-027) when the test selects the Treadmill chip. Will be revalidated when FT-027 lands.

## Implementation
- `frontend/projects/components/src/lib/chip/chip.component.{ts,html,scss}` — wraps `<mat-chip-option>`.
- Inputs: `selected`, `disabled`, `value`, `testid`. Standard `[testid]` → `[attr.data-testid]` forwarding.
- `<ng-content>` projects the chip label.
- Consumers compose `<mat-chip-listbox>` containing `<forge-chip>` instances; the inner `<mat-chip-option>` participates in the listbox's selection state.
- Public-API export added.

## Library placement
- `components` library, no imports from `api` / `domain`. ✅

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | `components` placement | pass |
| 2 | Material 3 wrapper | pass — `<mat-chip-option>` |
| 3 | `[selected]` modifier supported | pass |
| 4 | `[testid]` forwarded | pass |
| 5 | Per-file split | pass |
| 6 | Public API export | pass |
