---
slice: BT-026
title: List rewards catalog (GET /api/rewards)
status: pass
date: 2026-05-10
---

# BT-026 — List rewards catalog

## Acceptance test
`backend/tests/Forge.Acceptance/Rewards/ListRewardsAcceptanceTest.cs` — one scenario:
- Register, flip the lowest-SortOrder seeded reward to `IsActive = false`, then GET `/api/rewards`. Response excludes the inactive row, contains ≥6 active rows, and is ordered ascending by `SortOrder`.

## Implementation
- `RewardItemDto(Guid Id, string Name, string Description, int CostPoints, int SortOrder)`
- `ListRewardsQuery() : IRequest<IReadOnlyList<RewardItemDto>>`
- `ListRewardsQueryHandler` depends on `IAppDbContext`; uses `.AsNoTracking()`, filters by `IsActive`, orders by `SortOrder`, and projects to DTO
- `[Authorize] [HttpGet]` on `RewardsController` returns `Ok(items)`

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | One handler per query | pass |
| 2 | `.AsNoTracking()` | pass |
| 3 | Inactive rows hidden | pass |
| 4 | Stable order (SortOrder ascending) | pass |
| 5 | DTO projection (no entity leak) | pass |
| 6 | Authenticated only | pass |

## Test results
- Backend full suite: 38/38 pass.
