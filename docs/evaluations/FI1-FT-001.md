# FI1 — FT-001 — Wrap `CardComponent` around `<mat-card>`

## Pass 1 - findings

Walked the Implementation Evaluation Rubric (criteria 1–10) against the FT-001 implementation. Scope per FI1: Frontend, Library Structure, Authentication (frontend side), Testing, and General sections.

### Mechanical checks

- `card.component.ts` declares exactly one top-level type (`CardComponent`); the file is paired with `card.component.html` and `card.component.scss` — one-type-per-file with the .ts/.html/.scss triple intact (criterion 5 ✅).
- No `template:` / `styles:` inline, no `TODO` / `FIXME` / `console.log` introduced (criteria 4, 5 ✅).
- BEM hooks preserved: `card`, `card__header`, `card__title`, `card__body` still present on the host elements so consumers retain a stable selector surface even though Material now provides the visuals.
- Public API of `projects/components/src/public-api.ts` unchanged — no consumer in `domain` or `forge` needs to change.
- `ng build forge --configuration=development` → 0 errors, 0 warnings.
- `npx playwright test` → `1 passed (4.9s)` against the live backend.

### Library placement (criterion 7)

`CardComponent` continues to live in `projects/components/src/lib/card/`. The new import is `MatCardModule` from `@angular/material/card` — an external Angular Material package, not a workspace `api` / `domain` import. Verified by `grep -E "from ['\"](api|domain|forge)['\"]" projects/components` returning zero matches. ✅

### Guidance adherence (criterion 1)

- "Angular Material components for every component" — `CardComponent` now wraps `<mat-card appearance="outlined">` with `<mat-card-header>` + `<mat-card-title>` + `<mat-card-content>` (Frontend ✅).
- BEM naming preserved on the host elements (Frontend ✅).
- `mat.theme(...)` (already wired in MF1's `styles.scss`) drives Material's M3 palette so the outlined card renders in the Forge Fit color roles automatically — no manual color overrides in the component SCSS (the SCSS is now a documentation-only stub).
- One type per file (General ✅).

### Requirements coverage (criterion 2)

FT-001 ships the full slice: template swap to Material, SCSS pruned of the now-Material-provided styles, public-api.ts unchanged. The existing `<forge-card title="Server status">` instance on the dashboard renders correctly. No partial implementation.

### Radically simple (criterion 3)

The new SCSS is empty except for inline comments documenting why each BEM block is intentionally bare (Material owns the visuals). No speculative abstractions, no defensive guards, no leftover MVP-era styling.

### ATDD evidence (criterion 8)

The gating spec is the existing `tests/sign-in.spec.ts` (committed in MF1 at `e7f9234`, before this implementation). It asserts the dashboard renders the `<forge-card title="Server status">` and the embedded `<forge-health-badge>` reads "Healthy". After the swap, the spec passes unchanged at `1 passed (4.9s)` — the Material-wrapped card still satisfies the same DOM / data-testid contract.

### Mobile-first + responsive (criterion 9)

Captured the dashboard at 360 / 768 / 1440 widths via a one-off Playwright script (artifacts removed after inspection). Dashboard `<forge-card>` renders cleanly at all three breakpoints with the new Material outlined variant — title typography and the health-badge content sit correctly inside Material's `<mat-card-content>` padding. The card grid behaviour from MF2 (single column at <768, two columns at ≥768) is unchanged.

### Build / run clean (criterion 10)

- `dotnet build` (backend) — green from prior MB2 work; not retouched by this slice.
- `ng build forge` — 0 errors / 0 warnings, bundle size moved from 1.55 MB to 1.57 MB (the `MatCardModule` addition; well within the `maximumWarning: 500 kB / maximumError: 1 MB` initial-chunk budgets in `angular.json`).
- `npx playwright test` — 1 passed against the live backend.

Pass 1 produces zero blocking findings. FT-001 is complete.
