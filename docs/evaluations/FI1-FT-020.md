---
slice: FT-020
title: StreakCardComponent (`<forge-streak-card>`)
status: pass
date: 2026-05-10
---

# FT-020 — StreakCardComponent

## Acceptance test
End-to-end coverage in `dashboard.spec.ts` (FT-024). FT-020 itself is a domain card; the app build verifies the wiring.

## Implementation
- `frontend/projects/domain/src/lib/streak-card/streak-card.component.{ts,html,scss}`.
- Injects `IDashboardService` via `DASHBOARD_SERVICE`.
- State held in signals. Computed `multiplier = min(1.50, 1 + 0.01 × currentStreak)` mirrors the BT-024 backend formula so the displayed badge label matches the points granted.
- `multiplierLabel` formatted as `×1.07`. `badgeVariant` is `success` when streak > 0, else `neutral`.
- Composes `<forge-card>` + `<forge-badge>` (both from `components`).
- BEM class names; `data-testid` on the card, days value, and multiplier badge.
- Public-API export added.

## Library placement
- `domain` library, imports `api` + `components` only. ✅

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | `domain` placement | pass |
| 2 | Injects via `DASHBOARD_SERVICE` | pass |
| 3 | Composes `CardComponent` + `BadgeComponent` | pass |
| 4 | Streak multiplier mirrors BT-024 formula | pass |
| 5 | State held in signals | pass |
| 6 | Per-file split | pass |
| 7 | Public API export | pass |
| 8 | App build | pass |
