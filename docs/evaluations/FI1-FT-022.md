---
slice: FT-022
title: TierCardComponent + RewardsService.getCurrentTier
status: pass
date: 2026-05-10
---

# FT-022 — TierCardComponent + RewardsService.getCurrentTier

## Acceptance test
End-to-end coverage in `dashboard.spec.ts` (FT-024). The slice itself plumbs the new rewards service and a domain card.

## Implementation
### `api` library
- `models/tier.model.ts` — `Tier { name, lifetimePoints, nextTierName, pointsToNextTier }` matching the BT-028 backend payload.
- `rewards.service.contract.ts` — `IRewardsService` (initial method `getCurrentTier()`) + `REWARDS_SERVICE` token.
- `rewards.service.ts` — `RewardsService` HTTP impl calling `GET ${API_BASE_URL}/api/tier`.
- Public-API exports added.

### `domain` library
- `tier-card/tier-card.component.{ts,html,scss}`.
- Injects both `DASHBOARD_SERVICE` (for `currentBalance`) and `REWARDS_SERVICE` (for tier name + next-tier hints).
- Renders the tier name, current balance ("X pts available"), and a progress label ("Y pts to NextTier" or "Top tier reached" when at the top).
- State held in signals.
- BEM class names; `data-testid` attributes for FT-024 to assert.
- Public-API export added.

### `app` config
- `{ provide: REWARDS_SERVICE, useClass: RewardsService }` registered alongside the existing service bindings.

## Library placement
- Service in `api`, paired with contract + token. ✅
- Card in `domain`, imports `api` + `components` only. ✅

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | Service in `api` library with contract + token | pass |
| 2 | Card in `domain` library | pass |
| 3 | Card injects both `DASHBOARD_SERVICE` and `REWARDS_SERVICE` | pass |
| 4 | State held in signals | pass |
| 5 | DI registered in app | pass |
| 6 | Public API exports | pass |
| 7 | App build | pass |
