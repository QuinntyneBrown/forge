---
slice: FT-018
title: DashboardService + IDashboardService contract
status: pass
date: 2026-05-10
---

# FT-018 — DashboardService + contract

## Acceptance test
Per the FT-018 entry, end-to-end coverage lands with `dashboard.spec.ts` (FT-024). FT-018 itself is plumbing — the build verifies the api library compiles with the new symbol surface, and the existing 12-test Playwright regression continues to pass.

## Implementation
- `frontend/projects/api/src/lib/models/dashboard-summary.model.ts` — `DashboardSummary` + `NextRewardDto` interfaces matching the BT-029 backend response (camel-case JSON property names: `caloriesToday`, `targetCalories`, `minutesToday`, `targetMinutes`, `currentStreak`, `currentBalance`, `lifetimePoints`, `tier`, `nextRewardWithinReach`, `monthToDateWeightLossLb`, `monthlyWeightGoalLb`).
- `frontend/projects/api/src/lib/dashboard.service.contract.ts` — `IDashboardService` with `getSummary(): Observable<DashboardSummary>` and the `DASHBOARD_SERVICE` injection token.
- `frontend/projects/api/src/lib/dashboard.service.ts` — `DashboardService` implementation calling `GET ${API_BASE_URL}/api/dashboard`.
- Public-API exports: model, contract, service all surfaced from `projects/api/src/public-api.ts`.
- DI registration: `{ provide: DASHBOARD_SERVICE, useClass: DashboardService }` in `projects/forge/src/app/app.config.ts`.

## Library placement
- Lives entirely in the `api` library — backend-facing service paired with its `*.service.contract.ts` and injection token. ✅
- App registers via the abstract token; downstream domain components will inject `IDashboardService` via `inject(DASHBOARD_SERVICE)`.

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | Service in `api` library | pass |
| 2 | Paired contract + injection token | pass |
| 3 | DTO model in `api/lib/models/` | pass |
| 4 | DI registered in app | pass |
| 5 | Public API exported | pass |
| 6 | Compiles cleanly | pass |
