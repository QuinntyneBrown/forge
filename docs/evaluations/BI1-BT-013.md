---
slice: BT-013
title: List equipment (GET /api/equipment)
status: pass
date: 2026-05-10
---

# BT-013 — List equipment

## Acceptance test
`backend/tests/Forge.Acceptance/Equipment/ListEquipmentAcceptanceTest.cs` — one scenario:
- Anonymous GET returns four `EquipmentDto { Id, Name }` rows in stable enum order: Treadmill, IndoorBike, BenchPress, Elliptical, with display names Treadmill / Indoor Bike / Bench Press / Elliptical.

## Implementation
- `EquipmentDto(string Id, string Name)` (Id is the enum name; Name is the display label)
- `ListEquipmentQuery() : IRequest<IReadOnlyList<EquipmentDto>>`
- `ListEquipmentQueryHandler` returns the four entries directly — equipment is fixed reference data, no DB read needed
- `EquipmentController` is open (no `[Authorize]`) since this is reference metadata the unauthenticated sign-up screen will need

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | One handler per query | pass |
| 2 | Stable enum order | pass — array literal mirrors `EquipmentType` numeric order |
| 3 | DTO is JSON-friendly (no entity leak) | pass |
| 4 | No stubs / TODOs | pass |

## Test results
- Backend full suite: 23/23 pass (1 new BT-013 test + 22 prior).
