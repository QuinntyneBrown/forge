---
slice: FT-003
title: IconButtonComponent (`<forge-icon-button>`)
status: pass
date: 2026-05-10
---

# FT-003 — IconButtonComponent

## Acceptance test
Per the FT-003 entry in `docs/plans/frontend-tasks.md`, no standalone Playwright spec is required — the component is verified indirectly when its first consumer ships (FT-027 / three-dot menu, FT-031 / password reveal). The 12-test frontend regression continues to pass with the new component compiled into the `components` library bundle.

## Implementation
- `frontend/projects/components/src/lib/icon-button/icon-button.component.{ts,html,scss}` — wraps `<button mat-icon-button>` with the standard `[testid]` → `[attr.data-testid]` forwarding and an `[ariaLabel]` input forwarded to `[attr.aria-label]` so consumers can give the icon button a screen-reader name.
- 48×48 dp hit area enforced via `.icon-button { width: 48px; height: 48px; ... }` per L2-046 / Material 3 minimum touch target.
- `ng-content` projects the Material Symbols glyph (or any inner content) so consumers can drop in `<mat-icon>more_vert</mat-icon>` etc.
- Public-API export added to `projects/components/src/public-api.ts` so the symbol is reachable from `domain` and `app`.

## Library placement
- Lives in `components` (presentation primitive). No imports from `api` or `domain` — verified via the imports in the `.ts` file. ✅

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | Reusable presentation component → `components` library | pass |
| 2 | Material 3 wrapper preserved | pass — `<button mat-icon-button>` |
| 3 | `[testid]` forwarded to inner element via `[attr.data-testid]` | pass |
| 4 | 48×48 hit area | pass |
| 5 | Per-file split (.ts / .html / .scss) | pass |
| 6 | Public API export | pass |
| 7 | No imports from `api` / `domain` | pass |

## Test results
- Frontend full Playwright regression: 12/12 pass (no new spec for FT-003 by design).
