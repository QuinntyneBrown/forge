---
slice: FT-026
title: EquipmentService + IEquipmentService contract
status: pass
date: 2026-05-10
---

# FT-026 — EquipmentService + contract

## Acceptance test
Per the FT-026 entry, end-to-end coverage lands with FT-027 / FT-028. Plumbing only.

## Implementation
- `frontend/projects/api/src/lib/models/equipment-type.model.ts` — re-exports the `EquipmentType` literal-union (`Treadmill | IndoorBike | BenchPress | Elliptical`) under the alias `EquipmentTypeId` and adds `EquipmentItem { id, name }` matching the BT-013 backend payload.
- `frontend/projects/api/src/lib/equipment.service.contract.ts` — `IEquipmentService.list()` + `EQUIPMENT_SERVICE` token.
- `frontend/projects/api/src/lib/equipment.service.ts` — `EquipmentService` HTTP impl calling `GET ${API_BASE_URL}/api/equipment` (BT-013 endpoint is anonymous; the service still goes through the auth-aware HttpClient — backend simply doesn't require the bearer).
- Public-API exports added.
- `app.config.ts` registers `{ provide: EQUIPMENT_SERVICE, useClass: EquipmentService }`.

## Library placement
- `api` library, paired with contract + token. ✅

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | Service in `api` | pass |
| 2 | Paired contract + token | pass |
| 3 | Equipment model in `api/lib/models/` | pass |
| 4 | DI registered | pass |
| 5 | Public API exports | pass |
| 6 | App build | pass |
