---
slice: FT-010
title: EmptyStateComponent (`<forge-empty-state>`)
status: pass
date: 2026-05-10
---

# FT-010 — EmptyStateComponent

## Acceptance test
Per the FT-010 entry, the dedicated spec is `empty-state.spec.ts` (FT-034). FT-010 ships with FT-027 (zero-row variant) or FT-034 — neither has landed yet, so verification is deferred to whichever consumer lands first.

## Implementation
- `frontend/projects/components/src/lib/empty-state/empty-state.component.{ts,html,scss}` — pure layout primitive (no Material wrapper) per FP1 §3.
- Inputs: `title`, `message`, `testid`.
- Two `<ng-content>` slots:
  - `<ng-content select="[empty-state-illustration]"></ng-content>` for an illustration / glyph.
  - Default `<ng-content></ng-content>` for the primary CTA (e.g. `<forge-button>`).
- BEM-named classes: `.empty-state`, `.empty-state__illustration`, `.empty-state__title`, `.empty-state__message`, `.empty-state__cta`.
- Public-API export added.

## Library placement
- `components` library, no imports from `api` / `domain`. ✅

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | `components` placement | pass |
| 2 | Hand-rolled (intentional non-Material) | pass — FP1 §3 |
| 3 | `[title]` / `[message]` inputs | pass |
| 4 | Illustration slot via named ng-content | pass |
| 5 | CTA slot via default ng-content | pass |
| 6 | BEM class names | pass |
| 7 | Per-file split | pass |
| 8 | Public API export | pass |
