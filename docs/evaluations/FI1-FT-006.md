---
slice: FT-006
title: SwitchComponent (`<forge-switch>`)
status: pass
date: 2026-05-10
---

# FT-006 — SwitchComponent

## Acceptance test
Per the FT-006 entry in `docs/plans/frontend-tasks.md`, no standalone Playwright spec is required — the component is verified inside `profile.spec.ts` (FT-031), which toggles the kitchen-closes nudge and the leaderboard opt-in. Profile spec is part of the existing 12-test suite that continues to pass.

## Implementation
- `frontend/projects/components/src/lib/switch/switch.component.{ts,html,scss}` — wraps `<mat-slide-toggle>` with the standard `[testid]` → `[attr.data-testid]` forwarding.
- Inputs: `checked`, `disabled`, `testid`. Output: `checkedChange` (boolean) emitted from the inner `(change)` handler.
- `<ng-content>` projects the label text inside `.switch__label` so consumers can pass any inline content.
- Public-API export added to `projects/components/src/public-api.ts`.

## Library placement
- Lives in `components` (presentation primitive). No imports from `api` or `domain`. ✅

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | Reusable presentation component → `components` library | pass |
| 2 | Material 3 wrapper preserved | pass — `<mat-slide-toggle>` |
| 3 | `[testid]` forwarded via `[attr.data-testid]` | pass |
| 4 | Two-way friendly with `[checked]` + `(checkedChange)` | pass |
| 5 | Per-file split | pass |
| 6 | Public API export | pass |
| 7 | No imports from `api` / `domain` | pass |

## Test results
- Frontend Playwright regression last run before this slice: 12/12 pass. The component-only addition compiles into the library bundle without changing any behaviour exercised by existing specs.
