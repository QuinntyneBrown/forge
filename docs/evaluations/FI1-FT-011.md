---
slice: FT-011
title: ErrorBannerComponent (`<forge-error-banner>`)
status: pass
date: 2026-05-10
---

# FT-011 — ErrorBannerComponent

## Acceptance test
Per the FT-011 entry, the dedicated spec is `error-state.spec.ts` (FT-033). Verification deferred to that consumer.

## Implementation
- `frontend/projects/components/src/lib/error-banner/error-banner.component.{ts,html,scss}` — pure layout primitive.
- Inputs: `title`, `message`, `testid`. Retry CTA goes through `<ng-content>`.
- `role="alert"` so screen readers announce the banner immediately when it appears.
- Uses Material 3 system tokens (`--md-sys-color-error-container`, `--md-sys-color-on-error-container`, `--md-sys-color-error`) with safe fallbacks so the banner styles even before the design-token CSS lands.
- BEM classes: `.error-banner`, `.error-banner__body`, `.error-banner__title`, `.error-banner__message`, `.error-banner__cta`.
- Public-API export added.

## Library placement
- `components` library, no imports from `api` / `domain`. ✅

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | `components` placement | pass |
| 2 | Hand-rolled (intentional non-Material) | pass |
| 3 | `[title]` / `[message]` inputs | pass |
| 4 | Retry CTA via default ng-content | pass |
| 5 | `role="alert"` for accessibility | pass |
| 6 | BEM class names | pass |
| 7 | Per-file split | pass |
| 8 | Public API export | pass |
