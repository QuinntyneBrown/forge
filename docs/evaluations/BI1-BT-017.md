---
slice: BT-017
title: Set monthly weight goal (PUT /api/profile/weight-goal)
status: pass
date: 2026-05-10
---

# BT-017 — Set monthly weight goal

## Acceptance test
`backend/tests/Forge.Acceptance/Profile/SetMonthlyWeightGoalAcceptanceTest.cs` — three scenarios:
1. PUT 15 → 204; subsequent GET /api/me reports `monthlyWeightGoalLb: 15`.
2. PUT 0 → 400 ProblemDetails (FluentValidation `InclusiveBetween(1, 30)`).
3. PUT 31 → 400 ProblemDetails (same rule).

## Implementation
- `SetMonthlyWeightGoalCommand(int MonthlyWeightGoalLb) : IRequest`
- Validator: `RuleFor(x => x.MonthlyWeightGoalLb).InclusiveBetween(1, 30)`
- Handler: depends on `IAppDbContext` + `ICurrentUser`; loads the user, mutates `MonthlyWeightGoalLb`, saves
- `[Authorize] [HttpPut("weight-goal")]` on `ProfileController` returning `204 NoContent`

## Rubric
| # | Criterion | Result |
|---|-----------|--------|
| 1 | One handler per command, no repository/UoW | pass |
| 2 | Validation surfaces 400 ProblemDetails | pass — global middleware handles `ValidationException` |
| 3 | Authenticated only | pass — `[Authorize]` on controller |
| 4 | Mutates only the target field | pass — handler sets `MonthlyWeightGoalLb` and nothing else |
| 5 | No stubs / TODOs | pass |

## Test results
- Backend full suite: 26/26 pass (3 new BT-017 tests + 23 prior).
